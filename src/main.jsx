import React from "react";
import ReactDOM from "react-dom/client";
import LocalFlash from "./LocalFlash.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LocalFlash />
  </React.StrictMode>
);
